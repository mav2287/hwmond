# Apple Xserve BMC: KCS interface at I/O port 0xCA2
# Apple doesn't advertise the BMC via ACPI or SMBIOS Type 38,
# so ipmi_si can't auto-detect it. This tells the driver where it is.
options ipmi_si type=kcs ports=0xca2
